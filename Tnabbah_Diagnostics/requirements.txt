# Pinned for reproducible installs (Python 3.11+; 3.13 tested).
# UI is a static HTML/JS SPA served by FastAPI from /web — no JS build step required.
fastapi==0.104.1
numpy==2.2.1
pandas==2.2.3
pydantic==2.13.2
pytest==8.3.5
python-dotenv==1.0.0
uvicorn==0.24.0
# httpx is needed by FastAPI's TestClient and by tests/test_api_scan.py.
# Allow 0.27 or 0.28 — openai 1.55+ supports both (older openai breaks on 0.28).
httpx>=0.27,<0.30
emoji>=2.14.0
# openai SDK: 1.55+ uses `proxy=` kwarg compatible with httpx 0.28+.
# Older versions (e.g. 1.3.5) crash with: Client.__init__() got an unexpected
# keyword argument 'proxies' when httpx >= 0.28 is installed.
openai>=1.55.0
